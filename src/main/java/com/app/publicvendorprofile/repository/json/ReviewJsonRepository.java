package com.app.publicvendorprofile.repository.json;

/**
 * JSON repository removed — use JPA repositories instead.
 * This stub exists to avoid accidental direct usage; prefer ReviewRepository.
 */
public final class ReviewJsonRepository {
    public ReviewJsonRepository(Object loader) {
        throw new UnsupportedOperationException("ReviewJsonRepository removed. Use ReviewRepository (JPA) instead.");
    }
}
