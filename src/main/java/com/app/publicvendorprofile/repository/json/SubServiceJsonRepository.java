package com.app.publicvendorprofile.repository.json;

/**
 * JSON repository removed — use JPA repositories instead.
 * This stub exists to avoid accidental direct usage; prefer SubServiceRepository.
 */
public final class SubServiceJsonRepository {
    public SubServiceJsonRepository(Object loader) {
        throw new UnsupportedOperationException("SubServiceJsonRepository removed. Use SubServiceRepository (JPA) instead.");
    }
}
