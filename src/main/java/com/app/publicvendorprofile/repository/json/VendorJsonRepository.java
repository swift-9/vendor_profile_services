package com.app.publicvendorprofile.repository.json;

/**
 * JSON repository removed — use JPA repositories instead.
 * This stub exists to avoid accidental direct usage; prefer VendorProfileRepository.
 */
public final class VendorJsonRepository {
    public VendorJsonRepository(Object loader) {
        throw new UnsupportedOperationException("VendorJsonRepository removed. Use VendorProfileRepository (JPA) instead.");
    }
}
